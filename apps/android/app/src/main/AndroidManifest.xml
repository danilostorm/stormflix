<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />

    <!-- Generic Android player handoff remains optional. DLNA/UPnP discovery is
         implemented directly by StormFlix and does not depend on another app. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="http" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" />
        </intent>
    </queries>

    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />
    <uses-feature android:name="android.software.leanback" android:required="false" />

    <application
        android:allowBackup="true"
        android:banner="@drawable/tv_banner"
        android:icon="@drawable/ic_stormflix"
        android:label="StormFlix"
        android:supportsRtl="true"
        android:theme="@style/Theme.StormFlix"
        android:usesCleartextTraffic="true">

        <meta-data
            android:name="com.google.android.gms.cast.framework.OPTIONS_PROVIDER_CLASS_NAME"
            android:value="cloud.stormflix.app.CastOptionsProvider" />

        <!-- One player for every Android form factor: Web Playback Engine in a
             native fullscreen WebView, launched by the existing native UI. -->
        <activity
            android:name=".PlayerActivity"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize|smallestScreenSize|screenLayout"
            android:screenOrientation="sensorLandscape"
            android:supportsPictureInPicture="true"
            android:resizeableActivity="true"
            android:exported="false" />

        <activity android:name=".MusicActivity" android:exported="false" />
        <activity android:name=".CategoryBrowseActivity" android:exported="false" />
        <activity android:name=".SeriesBrowseActivity" android:exported="false" />
        <activity android:name=".SeriesDetailActivity" android:exported="false" />
        <activity android:name=".PersonActivity" android:exported="false" />
        <activity android:name=".DetailActivity" android:exported="false" />
        <activity android:name=".ProfileActivity" android:exported="false" />
        <activity android:name=".LoginActivity" android:exported="false" />

        <!-- Keep the complete native StormFlix catalog/navigation shell on
             phone, tablet, Android TV and Fire TV. It already contains the
             StormFlix brand, dynamic menus, search, profiles and TV focus UI. -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Retained as an internal full-Web shell for diagnostics/future use;
             it is no longer the launcher because the native catalog layout is
             now shared across all Android form factors. -->
        <activity
            android:name=".WebAppActivity"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize|smallestScreenSize|screenLayout"
            android:resizeableActivity="true"
            android:supportsPictureInPicture="true"
            android:exported="false" />
    </application>
</manifest>